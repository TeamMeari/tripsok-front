import styles from './HashtagBtnSkeleton.module.css';

const HashtagBtnSkeleton = () => {
    return (
        <div className={`${styles.hashtagBtnSkeleton} skeleton`}>
        </div>
    )
}

export default HashtagBtnSkeleton;